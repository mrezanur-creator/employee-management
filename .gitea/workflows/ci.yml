name: Employee Management CI

on:
  push:
    branches: [ "main", "master", "feature-employee-update" ]

permissions:
  contents: read
  packages: write
  
env:
  IMAGE_NAME: employee-management
  CHART_PATH: helm/employee-management

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Cache Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: maven-${{ hashFiles('**/pom.xml') }}

      - name: Build
        run: mvn clean verify
      # ------------------------------------------------
      # Generate Version
      # ------------------------------------------------
      - name: Generate Image Version
        id: versioning # Kita beri ID untuk dipanggil di step berikutnya
        shell: powershell
        run: |
          $sha=(git rev-parse --short HEAD)
          $version="1.0.$sha"
          
          # Menggunakan Workflow Commands (Aman dari isu encoding PowerShell)
          Write-Output "::set-output name=IMAGE_VERSION::$version"
          Write-Host "Version terpilih = $version"

      # ------------------------------------------------
      # Docker
      # ------------------------------------------------
      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build Docker
        shell: powershell
        run: |
          # Panggil menggunakan format steps.[ID_STEP].outputs.[NAMA_OUTPUT]
          docker build -t ghcr.io/${{ secrets.GHCR_USERNAME }}/employee-management:${{ steps.versioning.outputs.IMAGE_VERSION }} .

      - name: Push Docker
        shell: powershell
        run: |
          docker push ghcr.io/${{ secrets.GHCR_USERNAME }}/employee-management:${{ steps.versioning.outputs.IMAGE_VERSION }}

      # ------------------------------------------------
      # Update Helm
      # ------------------------------------------------
      - name: Update values.yaml
        shell: powershell
        run: |
          (Get-Content "${{ env.CHART_PATH }}/values.yaml") `
          -replace 'tag:.*','  tag: "${{ steps.versioning.outputs.IMAGE_VERSION }}"' |
          Set-Content "${{ env.CHART_PATH }}/values.yaml"

      # ------------------------------------------------
      # Helm Steps
      # ------------------------------------------------
      - name: Helm Lint
        run: helm lint ${{ env.CHART_PATH }}

      - name: Helm Package
        run: helm package ${{ env.CHART_PATH }}

      - name: Helm Repo Update
        run: helm repo update

      - name: Read Chart Version
        shell: powershell
        run: |
          $chart=(Select-String '^version:' "${{ env.CHART_PATH }}/Chart.yaml").Line.Split(":")[1].Trim()
          "CHART_VERSION=$chart" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Push Helm Chart
        shell: powershell
        run: |
          helm cm-push employee-management-${{ env.CHART_VERSION }}.tgz employee-management